# DSM 打印机 Bonjour 共享内部 API

## 标识

| 字段 | 值 |
| --- | --- |
| 端点或端点组标识 | `dsm-printer-bonjour-sharing` |
| 项目组件标识 | `dsm-core` |
| 所属范围 | DSM 控制面板 / 外接设备 |
| 能力名称 | 打印机 Bonjour 共享只读状态候选 |
| 分类 | `internal` |
| 操作性质 | `read` |
| 风险等级 | `medium`（响应数据类别未知） |

## 证据边界

当前唯一证据来自仓库既有的 DSM 前端静态目录整理，只确认
`SYNO.Core.ExternalDevice.Printer.BonjourSharing` 存在候选 `get` 方法。没有在当前
DSM 环境中观察到请求，也没有取得脱敏响应。因此版本、路径、请求格式、参数、响应
容器、字段、权限和错误语义全部未验证，客户端不发起该请求。

通用文件服务中的 Bonjour/Avahi 设置来自其他 API，不能等同、复用或推断为打印机
Bonjour 共享状态。

## 请求契约

| 字段 | 值 |
| --- | --- |
| API 名称 | `SYNO.Core.ExternalDevice.Printer.BonjourSharing` |
| 路径 | 未验证；未来必须由 `SYNO.API.Info` 运行时发现 |
| HTTP 方法 | 未观察；客户端通用 DSM API 使用 `POST` 不能替代端点证据 |
| API 版本 | 未验证，不假定 v1 |
| 候选方法 | `get` |
| 参数 | 未验证，不假定无参数 |
| 鉴权机制 | 未验证；不得记录 Cookie、SID、SynoToken 或其他值 |
| 内容类型 | 未验证 |

在版本、参数和响应字段至少取得官方静态契约或脱敏只读响应前，Apple、Android 和
Windows 均不注册该能力，不发送猜测请求。

## 响应与错误

成功容器和字段均未知。当前不能判断该端点表达以下哪一种语义：

- Bonjour 广播开关；
- 打印机共享功能是否可用；
- 是否至少存在一台已共享打印机；
- 包含打印机或队列清单的复合响应。

`false`、`null`、空对象、字段缺失、接口缺失、硬件不支持、未连接打印机、共享关闭和
无权限不能互相替代。任何未知字符串也不得通过宽松布尔转换误报为“已关闭”。

| 场景 | 当前证据 | 是否可重试 | 降级或恢复 |
| --- | --- | --- | --- |
| API 或版本未知 | `static` 候选 | 否 | 客户端保持关闭且零请求 |
| 无权限 | 错误码未验证 | 否 | 不推断管理员权限或共享状态 |
| 字段缺失/结构变化 | 响应未验证 | 否 | 不解释为关闭或没有打印机 |
| 网络失败 | 尚无客户端调用 | 不适用 | 不影响外接存储与其他 NAS 设置 |

## 版本验证

| 环境标识 | 证据等级 | 接口版本 | 结果 | 日期 | 证据路径 |
| --- | --- | --- | --- | --- | --- |
| `lab-a-dsm-7-2-1-69057-u12-20260729` | `static` | 未验证 | 仅登记静态 API 名称与 `get`；没有在该环境观察或调用 | 2026-08-03 | `docs/api/DSM_WEB_API_REFERENCE_ZH.md` |

静态目录不证明该端点在当前 DSM build 可发现或可读取。

## 能力探测与降级

- 启用条件：当前无；未来必须先确认服务端版本范围和客户端明确支持版本相交；
- 新版本默认行为：关闭，不选择服务端最高版本，不猜测参数或字段；
- 接口缺失、版本未知、权限不足或结构未知：单项不可用且零猜测请求；
- 字段缺失或类型变化：不得把未知值转换成 `false`；
- 替代的官方 API：当前未发现；
- 功能开关：没有客户端功能开关，能力整体关闭。

## 客户端与测试

- Apple / Android / Windows Adapter：未实现；
- 领域模型：未建立；取得证据后最多考虑只含可空布尔状态的独立快照；
- Schema：无真实响应，不建立响应 Schema；
- 脱敏 fixture：无真实响应，不创建 fixture；
- 自动化测试：当前无请求实现；未来必须覆盖版本拒绝、严格布尔、隐私字段忽略、
  能力缺失零请求、空/错误/正常态以及全局零写入；
- 产品兼容矩阵：`docs/compatibility/DSM_COMPATIBILITY_MATRIX.md`；
- 机器索引：`contracts/private-api/compatibility.json`。

## 安全与副作用

- 会读取的数据类别：未知；可能包含打印机或队列信息，因此先按中风险处理；
- 禁止进入领域模型、日志和界面的候选敏感字段：打印机名称、型号、序列号、USB 设备
  节点、队列/URI、主机/IP/MAC、位置、驱动、账号、共享名和路径；
- 可能产生的副作用：静态目录只列 `get`，未发现写方法，但不能据此反推不存在写 API；
- 所需权限：未验证；
- 重复提交保护与写后校验：当前无写能力，不适用；
- 临时数据清理：本批没有浏览器导出、响应、截图或调试文件。

## 未验证事项

- 当前 DSM build 是否通过 `SYNO.API.Info` 暴露该 API，以及真实路径和版本范围；
- `get` 的请求格式、参数、响应容器、布尔字段和权限错误；
- 服务不可用、无打印机、共享关闭与无权限的差异；
- 是否返回任何打印机、队列、网络或设备标识；
- 普通账号、不同硬件和 DSM 更新下的行为。
