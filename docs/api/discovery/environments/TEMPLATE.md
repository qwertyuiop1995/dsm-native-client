# 私有 API 发现环境快照

> 复制后重命名本文件；不得在模板或记录中填写 NAS 地址、IP、QuickConnect ID、序列号、账号或证书指纹。

## 基本信息

| 字段 | 值 |
| --- | --- |
| 环境标识 | `待填写，例如 lab-a-dsm-7-2-1-69057-u12-20260729` |
| 匿名设备别名 | `lab-a / lab-b；同一设备升级后保持不变` |
| 基线状态 | `current / historical` |
| 替代的旧基线 | `首次记录写 none；升级后填写旧环境标识` |
| 发现日期 | `YYYY-MM-DD` |
| DSM 版本 | `待填写` |
| DSM build | `待填写` |
| Update | `待填写；没有则写 none` |
| 设备架构类别 | `待填写；仅记录兼容所需类别` |
| 连接方式 | `局域网直连 / QuickConnect 直连 / QuickConnect 中继` |
| 证书类别 | `公共 CA / 私有 CA / 自签名` |
| 账号权限类别 | `管理员 / 普通用户 / 受限测试账号` |

## 相关套件

| 项目组件标识 | 显示名称 | 完整版本 | 运行状态 | 备注 |
| --- | --- | --- | --- | --- |
| `待填写` | `待填写` | `待填写` | `运行 / 停止 / 升级中` | `不得包含用户数据` |

## 证据来源

- [ ] `SYNO.API.Info` 脱敏能力发现。
- [ ] 已登录官方网页的只读网络请求。
- [ ] 官方公开文档。
- [ ] 官方前端静态资源中的候选接口。
- [ ] 客户端自动化契约测试。
- [ ] 专用测试环境的受控行为验证。

相关文档或 fixture：

- `待填写仓库相对路径`

## 发现范围

- 本次目标：
- 明确不检查：
- 当前限制：

## 安全检查

- [ ] 未保存 Cookie、SID、SynoToken、DID、OTP 或密码。
- [ ] 未保存 NAS 地址、IP、QuickConnect ID、序列号或证书指纹。
- [ ] 未保存用户名、共享名、路径、文件名、消息、日志正文或其他真实用户数据。
- [ ] 未执行未获授权的写操作。
- [ ] 临时抓包、截图和响应已在完成脱敏记录后删除。

## 结论

- 已确认：
- 未验证：
- 需要在其他版本或权限下复验：
