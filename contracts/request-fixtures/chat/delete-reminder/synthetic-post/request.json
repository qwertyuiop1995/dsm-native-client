{
  "schemaVersion": 1,
  "fixtureId": "chat.delete-reminder.synthetic-post",
  "module": "chat",
  "operation": "deleteReminder",
  "api": {
    "name": "SYNO.Chat.Post.Reminder",
    "method": "delete",
    "preferredVersion": 1,
    "resolvedVersion": 1,
    "resolvedPath": "entry.cgi"
  },
  "transport": { "httpMethod": "POST", "requestFormat": "form" },
  "parameters": [
    { "name": "post_id", "valueType": "string", "encodedValue": "synthetic-post" }
  ],
  "authentication": {
    "required": true,
    "synoTokenRequired": false,
    "sessionLocations": ["cookie", "form"],
    "synoTokenLocations": ["header", "form"]
  },
  "policy": {
    "retryPolicy": "queryStateBeforeDecision",
    "risk": "standardWrite",
    "readbackPolicy": "required"
  },
  "source": { "kind": "synthetic", "evidence": "sourceReviewed" }
}
