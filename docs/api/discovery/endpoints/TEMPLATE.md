# 私有 API 端点记录

## 标识

| 字段 | 值 |
| --- | --- |
| 端点或端点组标识 | `待填写；与 compatibility.json 一致` |
| 项目组件标识 | `待填写；与环境快照的组件标识一致` |
| 所属范围 | `DSM / 套件内部标识` |
| 能力名称 | `待填写稳定业务名称` |
| 分类 | `official / internal` |
| 操作性质 | `read / write` |
| 风险等级 | `low / medium / high / critical` |

## 请求契约

| 字段 | 值 |
| --- | --- |
| API 名称 | `逐项填写；紧密关联端点组可以包含多个名称` |
| 路径 | `逐项填写相对路径，不含主机` |
| HTTP 方法 | `GET / POST / WebSocket；可多选` |
| API 版本 | `待填写` |
| 鉴权机制 | `只描述 Cookie、请求头或会话机制，不记录值` |
| 内容类型 | `待填写` |

参数：

| 参数 | 类型 | 必需 | 含义 | 脱敏示例 |
| --- | --- | --- | --- | --- |
| `待填写` | `string` | 是 | `待填写` | `<redacted-value>` |

不得用翻译后的界面文字、用户数据或服务器返回的显示名称参与参数和业务判断。

## 响应与错误

成功响应结构：

```json
{
  "success": true,
  "data": {}
}
```

| 字段 | 类型 | 必需 | 版本差异 | 客户端处理 |
| --- | --- | --- | --- | --- |
| `success` | `boolean` | 是 | `待填写` | `待填写` |

错误与权限：

| 场景 | 错误语义 | 是否可重试 | 降级或恢复 |
| --- | --- | --- | --- |
| 无权限 | `待填写，不保存真实响应正文` | 否 | `隐藏能力或提示所需权限` |

## 版本验证

| 环境标识 | 证据等级 | 接口版本 | 结果 | 日期 | 证据路径 |
| --- | --- | --- | --- | --- | --- |
| `待填写` | `static / observed / read-verified / behavior-verified / failed` | `待填写` | `待填写` | `YYYY-MM-DD` | `待填写` |

## 能力探测与降级

- 启用条件：
- 新版本默认行为：
- 接口缺失：
- 字段缺失或类型变化：
- 权限不足：
- 网络失败：
- 替代的官方 API：
- 功能开关：

内部写接口在未记录的新 DSM build 或套件版本上默认关闭。内部只读接口必须可失败降级，不得阻断不依赖该接口的主流程。

## 客户端与测试

- Apple Adapter：
- Android Adapter：
- Windows Adapter：
- Schema：
- 脱敏 fixture：
- 自动化测试：
- 产品兼容矩阵条目：

## 安全与副作用

- 会读取的数据类别：
- 可能产生的副作用：
- 所需权限：
- 重复提交保护：
- 写后结果校验：
- 临时数据清理：

## 未验证事项

- `待填写`
