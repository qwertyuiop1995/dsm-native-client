# 项目协作规则

- 使用中文编写项目文档和代码注释。
- 优先使用 Synology 官方公开 API，内部 API 必须明确标注。
- 不提交密码、OTP、SID、SynoToken、Cookie、DID、证书私钥或真实用户数据。
- 不提交未脱敏的 HAR、PCAP、DSM 响应、文件路径和主机地址。
- 临时调试代码、临时抓包、临时测试账号资料和生成物在任务结束前删除。
- 正式自动化测试属于项目源码，可以保留；一次性调试文件不得提交。
- 修改 API 契约时评估并同步受影响的五端实现计划和兼容矩阵。
- 危险写操作必须具备确认、权限检查、重复提交保护和结果校验。

## 多 AI 模型协作与任务交接

- 仓库源码、`AGENTS.md`、契约、ADR、平台矩阵和可复现的测试结果是事实来源；其他模型的描述、历史对话和任务总结只能作为线索，存在冲突时必须重新检查仓库与验证结果。
- 开始修改前必须阅读适用范围内的 `AGENTS.md`，检查 `git status`、相关差异和目标文件；工作区可能包含用户或其他模型尚未提交的改动，不得擅自覆盖、回退、格式化或删除。
- 多个模型并行工作时必须拆分清晰且互不重叠的文件或模块范围；不得同时修改同一文件。写入前再次检查目标文件差异，发现并发变化时先合并上下文，不得用旧版本覆盖新内容。
- 延续项目现有架构、命名、资源键和实现模式。未经明确论证，不得为同一能力新增平行实现、重复抽象、临时兼容层或仅供某个模型使用的专有流程。
- 不得把推测当作事实，不得虚构 API、系统行为、测试结果或实机结论。无法验证的内容必须明确标注为“未验证”，并说明所需平台、设备或条件。
- 修改范围应与当前任务一致；发现相邻问题可以记录，但不得借机进行无关重构、批量格式化、依赖升级或配置迁移。
- 新增第三方依赖、改变工具链或最低系统版本、修改 Bundle ID、包名、签名、权限、数据格式、持久化结构或公开契约前，必须说明必要性、影响范围、迁移与回滚方案，并取得用户明确同意。
- 生成文件必须通过项目既定生成流程更新，不得直接手改；一次性脚本、日志、构建产物和模型生成的中间文件在任务结束前清理。
- 每次交付必须列出实际修改、关键决策、已运行的验证及未验证风险。测试必须报告真实命令和结果，不得以静态阅读、局部测试或其他平台通过代替目标平台构建与实机验收。
- 任务未完成而需要交给其他模型时，交接信息必须包含目标、已完成内容、改动文件、当前工作区状态、已运行验证、失败信息、剩余步骤和禁止触碰的用户改动，避免接手模型从头猜测或重复执行。
- 未经用户明确要求，不得执行 `git add`、提交、推送、创建或合并 PR，也不得改写提交历史；需要提交时只包含当前任务相关文件，并在操作前复核差异。
- 向外部 AI、搜索、日志或问题追踪系统提供内容时遵循最小披露原则，继续执行本文件的秘密与隐私规则，不上传真实凭据、用户数据、未脱敏响应或私有环境信息。

## UI/UX Pro Max 自动使用

- 任何会改变界面外观、布局、组件、导航、交互、动效、可访问性或用户体验的任务，都必须自动使用 `ui-ux-pro-max` Skill，不需要用户重复点名。
- 开始 UI/UX 任务前，先运行 `python3 tools/codex/ensure_ui_ux_pro_max.py`；Windows 使用 `python tools\codex\ensure_ui_ux_pro_max.py`。
- 引导脚本仅在全局 Skill 缺失时安装固定版本。安装后必须完整读取 `$CODEX_HOME/skills/ui-ux-pro-max/SKILL.md`；未设置 `CODEX_HOME` 时使用 `~/.codex/skills/ui-ux-pro-max/SKILL.md`。
- 新页面或整体重设计先生成 design system；局部组件、体验审查和 UI 缺陷按 Skill 路由使用对应领域检索。
- Apple、Android、Windows 分别使用 `swiftui`、`jetpack-compose`、`winui` 技术栈指南，不接受 Skill 中与本项目实际技术栈冲突的默认值。
- UI 建议必须保持平台原生体验，并满足浅色/深色模式、键盘、触控、VoiceOver/屏幕阅读器、动态文字和降低动态效果要求。
- Skill 只用于开发期设计支持，不得加入 App 运行时依赖，不得借 UI 修改改变 API、安全策略、Bundle ID、包名、会话存储或业务语义。
- 纯网络、协议、存储、构建、CI 或其他不影响界面的任务不运行该引导脚本。

## 面向用户的界面文案

- 所有面向终端用户的标题、字段、按钮、状态、提示和错误信息，默认按普通用户而不是开发者或协议调试人员的理解方式编写。
- 主流程不得直接暴露 API、HTTP 状态、能力发现、build、契约、Keychain、SID、SynoToken、证书钉扎等实现术语；确因安全核对需要展示指纹等技术信息时，必须先给出通俗解释，并将技术细节放在次级层级。
- 不得用“服务端处理、由 NAS 打包、递归、流式、分片、请求往返”等实现过程解释普通功能；默认只说明用户会得到什么、可能受到什么影响以及下一步怎么做。
- 错误信息必须说明“发生了什么”和“用户下一步可以怎么做”，不得只显示内部错误、错误码或无恢复路径的技术描述。
- 按钮名称应描述用户可预期的结果，例如“连接”“重新登录”“我已核对，继续连接”，避免使用内部动作名称。
- 新增或修改界面时必须审查全部可见文案；开发诊断信息只进入受控日志或可选的高级详情，不得混入默认用户界面。

## 国际化与本地化

- 所有新增或修改的用户可见文案必须通过平台语言资源提供，不得在 Swift、Kotlin、C#、SwiftUI、Compose 或 XAML 中新增硬编码显示文案。
- 初期支持英语与简体中文；每个用户界面资源键必须同时提供两种语言，缺少任一语言不得视为完成。
- 首次启动默认跟随系统首选语言；系统语言不属于英语或简体中文时必须回退英语。用户可固定选择英语或简体中文，选择结果在本机持久保存且不得与 NAS 配置、密码或会话绑定。
- 新功能必须同时覆盖标题、按钮、字段、状态、提示、错误、确认框、通知、菜单和无障碍文案，不需要用户再次提醒国际化要求。
- 翻译后的字符串不得参与业务判断、导航、筛选、持久化、API 参数或重复提交保护；领域层使用稳定枚举、错误类型或资源键，界面层负责本地化展示。
- 带变量、数量或时间的文案必须使用平台参数化、复数和区域格式能力，不得通过不利于翻译的字符串拼接生成完整句子。
- 日期、时间、数字和单位使用当前 App 语言格式。NAS 名称、用户名、文件名、聊天正文和服务器返回的用户数据保持原文。
- 增加语言时必须同步更新 `contracts/localization/supported-locales.json`、Apple、Android、Windows 资源、语言选择器、自动化测试、README 和平台矩阵。
- 提交前必须运行本地化资源完整性与硬编码扫描；新增缺失翻译、占位符不一致或用户界面硬编码时不得合并。
