{
  "schemaVersion": 1,
  "fixtureId": "vmm.get-task.synthetic-task",
  "module": "vmm",
  "operation": "getTask",
  "api": {
    "name": "SYNO.Virtualization.API.Task.Info",
    "method": "get",
    "preferredVersion": 1,
    "resolvedVersion": 1,
    "resolvedPath": "entry.cgi"
  },
  "transport": {"httpMethod": "POST", "requestFormat": "form"},
  "parameters": [
    {"name": "task_id", "valueType": "string", "encodedValue": "<synthetic-vmm-task>"}
  ],
  "authentication": {
    "required": true,
    "synoTokenRequired": false,
    "sessionLocations": ["cookie", "form"],
    "synoTokenLocations": ["header", "form"]
  },
  "policy": {"retryPolicy": "readOnlyAutomatic", "risk": "read", "readbackPolicy": "none"},
  "source": {"kind": "synthetic", "evidence": "publicDocumentation"}
}
